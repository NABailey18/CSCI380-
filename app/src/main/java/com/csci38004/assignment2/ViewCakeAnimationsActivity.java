package com.csci38004.assignment2;

import android.os.Bundle;
import android.support.annotation.Nullable;
import android.support.v7.app.AppCompatActivity;
import android.view.View;
import android.widget.ImageView;

public class ViewCakeAnimationsActivity extends AppCompatActivity {


    @Override
    protected void onCreate(@Nullable Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.cake_view_property_animations);

            /* create field for dog cakeview image that needs to be put into cake_view_animations.xml like
            ImageView dogImageView = findViewById(R.id.dogImageView);
            AKA WE NEED API FOR THIS
            */
    }
}
